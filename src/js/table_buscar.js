$(document).ready(function(){ 
    $('#datatable-default_length.dataTables_length > label').css('color', '#fff');
    $('#datatable-default2_length.dataTables_length > label').css('color', '#fff');
    $("input[type=search]").attr("placeholder","Buscar");
});
